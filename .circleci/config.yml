version: 2.1

orbs:
 win: circleci/windows@2.2.0

jobs:
  build:
    executor:
      name: win/default    
    steps:
      - checkout:
          path: C:\Users\circleci\project      
      - run:
          name: "Install project dependencies"
          command: dotnet.exe restore      
      - run:
          name: "Run Build step"
          command: cd UnitTestingAndMockingUsingXUnitAndMoq | msbuild
